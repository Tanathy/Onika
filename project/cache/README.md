# Cache

This folder is used for **runtime caches** created during training (for example: latents or preprocessing caches).

You normally don’t need to manage this manually—Onika will create and reuse files here to speed things up.
